{
  "name": "express",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "bin/www",
    "postinstall": "[ \"$NODE_ENV\" != production ] && npm run setup || true",
    "setup": "uglifyjs --version"
  },
  "engines": {
    "node": "6.0.0"
  },
  "dependencies": {
    "body-parser": "~1.0.0",
    "cookie-parser": "~1.0.1",
    "debug": "~0.7.4",
    "express": "~4.2.0",
    "heroku-destroy-temp": "1.0.1",
    "jade": "~1.3.0",
    "morgan": "~1.0.0",
    "static-favicon": "~1.0.0"
  }
}
