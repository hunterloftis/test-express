{
  "name": "express",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "bin/www",
    "postinstall": "[ \"$NODE_ENV\" != production ] && npm run setup || true",
    "setup": "uglifyjs --version"
  },
  "engines": {
    "node": "5.6.0"
  },
  "dependencies": {
    "express": "~4.2.0",
    "static-favicon": "~1.0.0",
    "morgan": "~1.0.0",
    "cookie-parser": "~1.0.1",
    "body-parser": "~1.0.0",
    "debug": "~0.7.4",
    "jade": "~1.3.0"
  }
}
